# Alternative Environment Variables with Currently Available Models
# Use this if the models specified in requirements are not available

# Required API Keys
GOOGLE_API_KEY=your_google_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Authentication (Required)
EDITOR_PASSWORD=change_this_password
APP_SECRET=generate_a_random_secret_key_here

# Model Configuration - Currently Available Models (as of 2024)
# Option 1: With -latest suffix
CLAUDE_MODEL=claude-3-5-sonnet-20241022
GEMINI_PRO=gemini-1.5-pro-latest
GEMINI_FLASH=gemini-1.5-flash-latest

# Option 2: Without -latest suffix (if above doesn't work)
# GEMINI_PRO=gemini-1.5-pro
# GEMINI_FLASH=gemini-1.5-flash

# Option 3: Models specified in requirements (might be future versions)
# CLAUDE_MODEL=claude-sonnet-4-5-20250929
# GEMINI_PRO=gemini-2.5-pro
# GEMINI_FLASH=gemini-2.5-flash

# Pipeline Configuration (Optional)
MAX_FORMATTER_RETRIES=1
MAX_INPUT_CHARS=150000
MODEL_TEMPERATURE=0.51
MODEL_TOP_P=0.95
MODEL_MAX_TOKENS=8000
MODEL_TIMEOUT=60

# Request Size Limit
MAX_REQUEST_SIZE_MB=10

# Rate Limiting
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_PERIOD=minute
