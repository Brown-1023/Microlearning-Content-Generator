# API Keys (Required)
GOOGLE_API_KEY=your_google_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Authentication (Required)
EDITOR_PASSWORD=change_this_to_editor_password
ADMIN_PASSWORD=change_this_to_admin_password
# Used for signing JWT tokens - generate a secure random string (e.g., using: openssl rand -hex 32)
APP_SECRET=generate_a_random_secret_key_here

# Model Configuration (Optional - defaults shown)
CLAUDE_MODEL=claude-sonnet-4-5-20250929
GEMINI_PRO=gemini-2.5-pro
GEMINI_FLASH=gemini-2.5-flash

# Pipeline Configuration (Optional)
MAX_FORMATTER_RETRIES=1
MAX_INPUT_CHARS=500000

# Server Configuration (Optional)
HOST=0.0.0.0
PORT=8000
RELOAD=False

# Rate Limiting (Optional)
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_PERIOD=minute